/**
 * lib — Global utilities and third-party wrappers
 *
 * This is the integration layer for external libraries.
 * Examples: date-fns helpers, Zod schemas, class-variance-authority setup.
 *
 * Keep this layer thin — only wrap or configure, never add business logic.
 */
