/**
 * Presentation — Providers
 *
 * React context providers that wrap the application or route segments.
 * Examples: QueryProvider (React Query), StoreProvider, ThemeProvider, AuthProvider.
 * Providers are consumed by Next.js layout.tsx files.
 */
